# Production build config — used automatically by `vite build`.
# This file IS committed (unlike .env, which is git-ignored), so the live API
# URL always reaches the server and gets baked into the production bundle.
VITE_API_URL=https://api.agpssvda.com/api
